1. build Commands for pyinstaller:
$pyinstaller main.py     # create build and distribution files
$pyinstaller --name botLW --onefile --windowed --icon=build-files\\icon.ico main.py

2.use inno setup:
--create a ino setup .iss file
--select executable from dist folder, add "image" directory for application files
--edit  Files Source for images , add "/images"


